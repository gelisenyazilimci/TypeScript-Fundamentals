let num = 5;

let a: number;
let b: string;
let c: boolean;
let e: number[];
let d: Array<number[]>;
let f: any[] = [1, false, "69"];
let h: [string, number, boolean] = ["a", 31, true]; // tuple


const krediPayment = 0;
const krediHavale = 1;
const eftPayment = 2;

enum Payment {payment, Havale, eft}

let kredi = Payment.payment; // 0
let kredhavale = Payment.Havale; // 1
let eft = Payment.eft; // 2

a = 5;
b = "deneme";
c = false;


let id: number | string = "31";
id = 1;
